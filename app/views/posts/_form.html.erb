<%= form_for(@post) do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h3><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h3>

      <ul>
      <% @post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="btn-group" data-toggle="buttons">
  <% Post.texts.each do |text,_| %>
    <label class="btn btn-default <%= 'active' if f.object.text == text %>">
      <%= text %>
      <%= f.radio_button :text,text %>
    </label>
  <% end%>
  </div>
  <div class="actions">
    <%= f.submit "Go!", class: 'btn btn-black' %>
  </div>
<% end %>
